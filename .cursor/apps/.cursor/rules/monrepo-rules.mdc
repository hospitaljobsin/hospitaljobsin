---
description: Rules to follow when adding packages to frontend projects
globs:
alwaysApply: true
---
- The projects in the `apps` directory are a part of a PNPM workspace at the root.
- Peer dependencies must be explicitly added to each project.
- Dependency installation takes place from the root, with PNPM filters to add to specific projects.
