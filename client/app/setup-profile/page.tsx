import { APP_NAME } from "@/lib/constants";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: `Set up Profile | ${APP_NAME}`,
};

export default function LoginPage() {
  return <></>;
}
